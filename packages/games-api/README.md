# @lunchtable/games-api

Headless HTTP API primitives for Lunch Table Games agent transports.

This package provides route manifests, JSON request helpers, response helpers,
typed fetch clients, and authoritative action submission guards that let humans,
local agents, hosted Eliza agents, MCP clients, and browser hosts share the same
legal-action surface.
