# @lunchtable/games-render

Renderer-neutral scene, camera, cue, interaction, and adapter descriptors for
Lunch Table Games.

```ts
import type { RenderSceneModel } from "@lunchtable/games-render";
```

Render adapters consume these models; rulesets remain authoritative for gameplay
state.
