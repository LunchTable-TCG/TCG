# @lunchtable/games-core

Deterministic Lunch Table Games runtime primitives for state shells, lifecycle
metadata, ruleset transitions, seeded randomness, and replay logs.

```ts
import type { GameRuleset } from "@lunchtable/games-core";
```

This package is renderer-neutral and does not depend on React, Convex, Pixi, or
game-specific card content.
